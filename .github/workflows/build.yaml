name: build_and_test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          # Need to clone everything to determine version from git.
          fetch-depth: 0
          cache: "pip"
          cache-dependency-path: "setup.cfg"

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"

      - name: Update pip/wheel infrastructure
        run: |
          python -m pip install --upgrade setuptools pip wheel

      - name: Install dependencies
        run: |
          python -m pip install -r requirements.txt

      # We have two cores so we can speed up the testing with xdist
      - name: Install pytest packages
        run: |
          python -m pip install "flake8<5" pytest pytest-flake8 pytest-xdist pytest-openfiles pytest-cov

      - name: List installed packages
        run: |
          python -m pip list -v

      - name: Build and install
        run: |
          python -m pip install --no-deps -v -e .

      - name: Run tests
        run: |
          pytest -r a -v --open-files --cov=python --cov=tests --cov-report=xml --cov-report=term --cov-branch
      - name: Upload coverage to codecov
        uses: codecov/codecov-action@v2
        with:
          file: ./coverage.xml
